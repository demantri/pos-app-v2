/**
 * Satu titik data untuk grafik dashboard. Semua grafik di sini berseri
 * tunggal, jadi tidak butuh legenda — judul kartunya yang menamai datanya.
 */
export type ChartPoint = {
    label: string;
    value: number;
};
